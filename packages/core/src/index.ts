/**
 * @glyph/core — public surface.
 *
 * Phase 0 exports:
 *   - version: package version constant
 *
 * Subsequent PRs will add: spec AST + Zod schemas, compiler, scenegraph,
 * SVG renderer, and the QueryHandle type.
 */
export const VERSION = "0.0.0";
