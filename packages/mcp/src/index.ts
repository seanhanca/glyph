/**
 * @glyph/mcp — Glyph MCP server.
 * Library export so other servers can embed Glyph's three tools.
 */
export { createServer, SERVER_NAME, SERVER_VERSION } from "./server.js";
export { ServerState } from "./state.js";
