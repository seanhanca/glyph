{
  "$schema": "../packages/core/dist/spec.schema.json",
  "data": {
    "source": "taxi.parquet",
    "transform": "SELECT pickup_hour, COUNT(*) AS rides FROM taxi GROUP BY pickup_hour ORDER BY pickup_hour"
  },
  "layers": [{ "mark": "bar", "encoding": { "x": "pickup_hour", "y": "rides" } }],
  "interactive": { "key": "pickup_hour" },
  "title": "Click a bar to drill into that hour"
}
